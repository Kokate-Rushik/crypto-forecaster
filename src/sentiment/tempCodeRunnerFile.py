 window)
    # if last_update_str:
    #     last_update = datetime.strptime(last_update_str, "%Y-%m-%d T %H:%M:%S")
    #     if (now - last_update) < timedelta(hours=6) and symbol_key in cache_data:
    #         print(f"⚡ Returning cached data for {symbol}...")
    #         return cache_data[sy